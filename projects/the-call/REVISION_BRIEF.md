# REVISION BRIEF — The Call

Project: `the-call`

Agent instructions: for each card below, apply the director's note by editing `production/generator/projects/the-call/matrix_data.json` (that project's single source of truth), then rerun `python3 production/generator/generate_prompts.py --project the-call`. After applying a note, set that card's status back to `pending` in `production/generator/projects/the-call/review_state.json` and clear or annotate the note. Card keys are stable: entity-id/state-id/view.

_No cards are currently marked as needing revision._